{
  "rules": {
    ".read": "auth != null",
    ".write": false,
    "players": {
      "$uid": {
        ".write": "auth != null && auth.uid == $uid",
        "purchasedSkins": {
          ".validate": "newData.isObject()"
        }
      }
    },
    "coins": {
      ".write": "auth != null",
      ".validate": "newData.hasChildren(['x', 'y'])"
    }
  }
}
